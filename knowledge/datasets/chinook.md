---
type: Dataset
title: Chinook
description: Luis Rocha's digital media store sample (v1.4.5, 2024-02-12) - 11 tables, 15,607 rows, official MySQL script, MIT-licensed, rich accented Latin text.
resource: https://github.com/lerocha/chinook-database
tags:
- tier-core
- mysql-native
- chinook
- mit
- encoding-canary
status: stable
trust: verified
stale_after: "2027-03-01"
generated:
  by: claude-code/claude-fable-5-1
  at: "2026-09-02T20:48:59Z"
verified:
- by: claude-code/claude-fable-5-1
  at: "2026-09-02T20:25:00Z"
sources:
- resource: https://raw.githubusercontent.com/lerocha/chinook-database/master/README.md
  title: README
  accessed: "2026-09-02"
- resource: https://raw.githubusercontent.com/lerocha/chinook-database/master/LICENSE.md
  title: LICENSE.md
  accessed: "2026-09-02"
- resource: https://api.github.com/repos/lerocha/chinook-database/releases/latest
  title: Release v1.4.5 assets
  accessed: "2026-09-02"
  version: v1.4.5 (2024-02-12)
- resource: https://github.com/lerocha/chinook-database/releases/download/v1.4.5/Chinook_MySql.sql
  title: Chinook_MySql.sql (inspected)
  accessed: "2026-09-02"
  version: md5 75acf33f91aebf8bf0d79f4458d43b79
- resource: https://dev.mysql.com/doc/refman/9.7/en/charset-national.html
  title: MySQL 9.7 national character set
  accessed: "2026-09-02"
---

# Identity
Chinook, a sample database representing a digital media store (artists, albums, tracks, playlists, customers, employees, invoices), by Luis Rocha; media data extracted from an iTunes library, customers/employees fictitious, four years of generated invoices ([README](/sources/github-lerocha-chinook-readme.md)). Version 1.4.5.

# Source artifact
* https://github.com/lerocha/chinook-database/releases/download/v1.4.5/Chinook_MySql.sql - 616,450 bytes, md5 `75acf33f91aebf8bf0d79f4458d43b79`, sha256 `02a1835417c3ea19bff64e1d1f22be738ce51b3f88b0473c24eebff9a3ccde4b`. Sibling `Chinook_MySql_AutoIncrementPKs.sql` 578,950 B. No auth; no upstream checksum ([release](/sources/github-lerocha-chinook-release-v1-4-5.md)).
* Master (2025-10-05) is 23 commits ahead but the MySQL script content is unchanged (only LF vs CRLF).

# Native format and friendlier forms
Native MySQL DDL+DML script generated by the project's T4 templates; also SQLite (.sqlite), PostgreSQL, SQL Server, Oracle, DB2 scripts and `ChinookData.json`. Nothing else is needed.

# Shape
Album 347, Artist 275, Customer 59, Employee 8, Genre 25, Invoice 412, InvoiceLine 2,240, MediaType 5, Playlist 18, PlaylistTrack 8,715, Track 3,503 - total 15,607 rows ([inspection](/sources/github-lerocha-chinook-mysql-script.md)).
* Encoding: UTF-8, no BOM, CRLF line endings (**verified 2026-09-02**: 15,876 CRLF, zero bare LF); 585 lines with non-ASCII (verified). All 11 row counts re-verified against the downloaded file; note the script uses one multi-row `INSERT INTO \`Table\` ... VALUES` per table with one tuple per line, so counting statements gives 1 per table and rows must be counted as tuples. Canaries: `Luís Gonçalves` (Customer 1), `Leonie Köhler`/`Theodor-Heuss-Straße 34` (2), `François Tremblay`/`Montréal` (3), `Bjørn Hansen`/`Ullevålsveien 14` (4), `František Wichterlová` (5), `Helena Holý` (6), `Astrid Gruber`/`Rotenturmstraße 4` (7); tracks `Antônio Carlos Jobim`, `Aria Mit 30 Veränderungen, BWV 988 "Goldberg Variations": Aria`; playlist `90’s Music` (U+2019); album `1º De Julho`.
* Size: 0.6 MB SQL; **Inferred** loaded size about 2-3 MB.

# Conversion path
Run the upstream script through the mysql client after a build-time rewrite of `NVARCHAR` to `VARCHAR ... CHARACTER SET utf8mb4` ([decision](/decisions/chinook-conversion-path.md)).

# Type-mapping hazards
* `NVARCHAR(n)` columns -> utf8mb3 in 9.7 with a deprecation warning ([manual](/sources/mysql-refman-9-7-charset-national.md)); rewrite to utf8mb4. `N'...'` literals are utf8mb3 introducers - keep (lossless for this data) or strip the `N`.
* `NUMERIC(10,2)` -> DECIMAL(10,2) (MySQL synonym); `DATETIME` literals written as `'2021/1/1'` - **Inferred:** accepted by MySQL's permissive date parsing; assert `SELECT MIN(invoicedate) FROM invoice` = `2021-01-01 00:00:00`.
* `CREATE DATABASE \`Chinook\`` has no charset; on Linux the name is case-sensitive - project proposes `chinook`.
* Mixed-case table/column names (`InvoiceLine`, `PlaylistTrack`) are case-sensitive with `lower_case_table_names=0`; the project lower-cases them ([naming convention](/decisions/database-naming-convention.md)) - the rewrite must cover CREATE TABLE, ALTER TABLE, CREATE INDEX and every INSERT column list consistently.
* Non-AUTO_INCREMENT integer PKs (explicit ids in INSERTs).

# Programmable objects
None upstream (no views, routines, triggers). 11 foreign keys `ON DELETE NO ACTION ON UPDATE NO ACTION` and 11 `IFK_*` indexes ported as-is.

# Indexing
Primary keys `PK_*`, secondary `IFK_*` on every FK column. Keep.

# Tests and expected values
**S-02 result (2026-09-02): green on the first attempt.** All 11 documented row counts matched the load exactly; 11 foreign keys, 0 orphans; every column is `utf8mb4` and no `NVARCHAR` survives; the 84 mixed-case identifiers are lower-cased with the map in `datasets/chinook/name_map.yaml`. Verified probes: customer 1 is `Luís` / `Gonçalves`, the playlist `90’s Music` keeps its curly apostrophe, and the upstream literal `'1962/2/18'` lands as `1962-02-18 00:00:00`. Baselines pinned in `datasets/chinook/tests/`; a from-scratch rebuild reproduces every fingerprint.

Row counts above; `SELECT firstname, lastname FROM customer WHERE customerid=1` = `Luís`, `Gonçalves`; `SELECT name FROM playlist WHERE playlistid=...` contains `90’s Music`; `SELECT CHARACTER_SET_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA='chinook' AND COLUMN_NAME='name' AND TABLE_NAME='track'` = `utf8mb4` after the rewrite; `SELECT SUM(total) FROM invoice` recorded at first load (no upstream checksum). Script md5 pinned.

# Tier assignment
core - 0.6 MB script, canonical Northwind alternative; evidence [release sizes](/sources/github-lerocha-chinook-release-v1-4-5.md).

# License and attribution
[MIT](/licenses/mit.md): keep "Chinook Database, Copyright (c) 2008-2024 Luis Rocha" and the permission notice ([LICENSE.md](/sources/github-lerocha-chinook-license.md)).

# Database name
`chinook` (upstream: `Chinook`). Per the [naming convention](/decisions/database-naming-convention.md) table and column identifiers are also lower-cased (`album`, `invoiceline`, `playlisttrack`; `albumid`, `invoicedate`), which the build script applies as a mechanical rewrite of the backticked identifiers in the upstream script (11 table names, all column names); documented as a deviation from upstream.

# Open questions
* Confirm `'2021/1/1'` DATETIME literal parsing under `SQL_MODE` defaults on 9.7 (cheap: load and check one row).
