---
type: Source
title: CO schema scripts at v23.3 (co_install.sql, co_create.sql, co_populate.sql, README.md)
description: DDL with identity columns and JSON check constraint, the 1.27 MB populate script, views, and the built-in row-count verification of the Customer Orders schema.
resource: https://github.com/oracle-samples/db-sample-schemas/tree/v23.3/customer_orders
tags:
- oracle
- co
- scripts
- json
status: stable
trust: verified
generated:
  by: claude-code/claude-fable-5-1
  at: "2026-09-02T20:25:00Z"
verified:
- by: claude-code/claude-fable-5-1
  at: "2026-09-02T20:25:00Z"
sources:
- resource: https://raw.githubusercontent.com/oracle-samples/db-sample-schemas/v23.3/customer_orders/co_install.sql
  title: co_install.sql (8,141 B)
  accessed: "2026-09-02"
  version: v23.3
- resource: https://raw.githubusercontent.com/oracle-samples/db-sample-schemas/v23.3/customer_orders/co_create.sql
  title: co_create.sql (19,570 B)
  accessed: "2026-09-02"
  version: v23.3
- resource: https://raw.githubusercontent.com/oracle-samples/db-sample-schemas/v23.3/customer_orders/co_populate.sql
  title: co_populate.sql (1,274,129 B, 8,980 lines)
  accessed: "2026-09-02"
  version: v23.3
- resource: https://raw.githubusercontent.com/oracle-samples/db-sample-schemas/v23.3/customer_orders/README.md
  title: customer_orders/README.md (2,384 B)
  accessed: "2026-09-02"
  version: v23.3
---

# What was read
All CO scripts at v23.3, downloaded and grepped. Encoding: `iconv -f UTF-8` validates co_populate.sql as UTF-8; it contains 6 lines with non-ASCII characters (ß ×2, ã ×2, é ×2, ó ×1), all in `stores.physical_address`.

# Relevant excerpt
* README: "Schema Version 21.1", "Release Date 08-FEB-2022", "19c and higher"; changes "All DATE data is updated", "New install mechanism".
* co_install.sql verification block: `customers 392, stores 23, products 46, orders 1950, shipments 1892, order_items 3914, inventory 566`.
* co_create.sql DDL (all identity columns are `INTEGER GENERATED BY DEFAULT ON NULL AS IDENTITY`): `customers(customer_id, email_address VARCHAR2(255 CHAR) NOT NULL UNIQUE, full_name VARCHAR2(255 CHAR) NOT NULL)`; `stores(store_id, store_name VARCHAR2(255 CHAR) NOT NULL UNIQUE, web_address VARCHAR2(100 CHAR), physical_address VARCHAR2(512 CHAR), latitude NUMBER(9,6), longitude NUMBER(9,6), logo BLOB, logo_mime_type/logo_filename/logo_charset VARCHAR2(512 CHAR), logo_last_updated DATE; CHECK (web_address IS NOT NULL or physical_address IS NOT NULL))`; `products(product_id, product_name VARCHAR2(255 CHAR) NOT NULL, unit_price NUMBER(10,2), product_details BLOB CHECK (product_details is json), product_image BLOB, image_mime_type/image_filename/image_charset VARCHAR2(512 CHAR), image_last_updated DATE)`; `orders(order_id, order_tms TIMESTAMP NOT NULL, customer_id FK, order_status VARCHAR2(10 CHAR) CHECK in ('CANCELLED','COMPLETE','OPEN','PAID','REFUNDED','SHIPPED'), store_id FK)`; `shipments(shipment_id, store_id FK, customer_id FK, delivery_address VARCHAR2(512 CHAR) NOT NULL, shipment_status VARCHAR2(100 CHAR) CHECK in ('CREATED','SHIPPED','IN-TRANSIT','DELIVERED'))`; `order_items(order_id FK, line_item_id INTEGER, product_id FK, unit_price NUMBER(10,2) NOT NULL, quantity INTEGER NOT NULL, shipment_id FK; PK (order_id, line_item_id); UNIQUE (product_id, order_id))`; `inventory(inventory_id, store_id FK, product_id FK, product_inventory INTEGER NOT NULL; UNIQUE (store_id, product_id))`. Indexes: customers_name_i, orders_customer_id_i, orders_store_id_i, shipments_store_id_i, shipments_customer_id_i, order_items_shipment_id_i, inventory_product_id_i. Views: `customer_order_products` (uses `LISTAGG(p.product_name, ', ' ON OVERFLOW TRUNCATE '...' WITH COUNT) WITHIN GROUP (ORDER BY oi.line_item_id)`), `store_orders` (`GROUP BY GROUPING SETS (...)` with `grouping_id`), `product_reviews` (`JSON_TABLE(p.product_details, '$' COLUMNS (NESTED PATH '$.reviews[*]' COLUMNS (rating INTEGER PATH '$.rating', review VARCHAR2(4000) PATH '$.review')))` plus `AVG(...) OVER (PARTITION BY product_name)`), `product_orders`. `COMMENT ON` for all tables, columns and views. No sequences, procedures or triggers.
* co_populate.sql: `SET DEFINE OFF; ALTER SESSION SET NLS_LANGUAGE=American;` explicit-id inserts inside PL/SQL blocks; counted INSERTs match the verification block. `orders.order_tms` values are `TO_TIMESTAMP('04-FEB-2021 13.20.22.245676861','DD-MON-YYYY HH24.MI.SS.FF')` — **all 1,950 rows carry 9 fractional-second digits**. `products.product_details` is `UTL_RAW.CAST_TO_RAW('{"colour":"white","gender":"Boy''s","brand":"COMTOURS","description":"...","sizes":["1 Yr",...],"reviews":[{"rating":8,"review":"..."},...]}')` (longest ≈1,350 characters; Lorem-ipsum text). `stores`: store 1 is `'Online'` with only `web_address 'https://www.example.com'`; the other 22 have multi-line `physical_address` literals with embedded newlines and lat/long (e.g. Berlin `'Behrenstraße 42 (Humboldt Carré)\n    10117 Berlin'`, São Paulo, Madrid `'C/ José Echegaray 6B'`, Vienna `'Donau-City-Straße 7'`, Mexico City `'Delegación Miguel Hidalgo'`). No `logo` / `product_image` values are inserted. After the data: `ALTER TABLE <t> MODIFY <id> GENERATED BY DEFAULT ON NULL AS IDENTITY (START WITH LIMIT VALUE)` for all six identity columns.

# What it was used to decide
[CO dataset record](/datasets/oracle-co.md); [JSON/timestamp fidelity question](/questions/oracle-co-json-and-timestamp-fidelity.md).
