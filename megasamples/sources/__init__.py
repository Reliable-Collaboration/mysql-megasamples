"""Translators and exporters for the upstream formats: T-SQL and PL/SQL install scripts, SQL Server
backups, CSV bundles, and the licence-gated loaders that fetch on the user's machine. Every module
here produces or reads the *source* side of a conversion; the MySQL side is megasamples/engines/mysql.
"""
