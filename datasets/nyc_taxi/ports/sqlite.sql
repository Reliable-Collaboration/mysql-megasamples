-- sqlite DDL for `nyc_taxi`, generated by megasamples from the MySQL corpus. Do not edit:
-- the next port rewrites it. Data is loaded from the MySQL Shell dump, not from here.

CREATE TABLE "taxi_zone" (
  "locationid" SMALLINT NOT NULL,
  "borough" VARCHAR(30) NOT NULL,
  "zone" VARCHAR(60),
  "service_zone" VARCHAR(20),
  PRIMARY KEY ("locationid")
);

CREATE TABLE "green_trips" (
  "trip_id" INTEGER PRIMARY KEY,
  "vendorid" SMALLINT,
  "pickup_datetime" DATETIME NOT NULL,
  "dropoff_datetime" DATETIME NOT NULL,
  "store_and_fwd_flag" CHAR(1),
  "ratecodeid" SMALLINT,
  "pulocationid" SMALLINT,
  "dolocationid" SMALLINT,
  "passenger_count" SMALLINT,
  "trip_distance" DOUBLE,
  "fare_amount" DECIMAL(10,2),
  "extra" DECIMAL(10,2),
  "mta_tax" DECIMAL(10,2),
  "tip_amount" DECIMAL(10,2),
  "tolls_amount" DECIMAL(10,2),
  "ehail_fee" DECIMAL(10,2),
  "improvement_surcharge" DECIMAL(10,2),
  "total_amount" DECIMAL(10,2),
  "payment_type" SMALLINT,
  "trip_type" SMALLINT,
  "congestion_surcharge" DECIMAL(10,2),
  "cbd_congestion_fee" DECIMAL(10,2),
  CONSTRAINT "fk_green_trips_do" FOREIGN KEY ("dolocationid") REFERENCES "taxi_zone" ("locationid") ON UPDATE NO ACTION ON DELETE NO ACTION,
  CONSTRAINT "fk_green_trips_pu" FOREIGN KEY ("pulocationid") REFERENCES "taxi_zone" ("locationid") ON UPDATE NO ACTION ON DELETE NO ACTION
);

CREATE INDEX "taxi_zone_ix_taxi_zone_borough" ON "taxi_zone" ("borough");
CREATE INDEX "green_trips_ix_green_trips_do" ON "green_trips" ("dolocationid");
CREATE INDEX "green_trips_ix_green_trips_pickup" ON "green_trips" ("pickup_datetime");
CREATE INDEX "green_trips_ix_green_trips_pu" ON "green_trips" ("pulocationid");
