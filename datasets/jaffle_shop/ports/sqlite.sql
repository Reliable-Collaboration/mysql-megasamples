-- sqlite DDL for `jaffle_shop`, generated by megasamples from the MySQL corpus. Do not edit:
-- the next port rewrites it. Data is loaded from the MySQL Shell dump, not from here.

CREATE TABLE "raw_customers" (
  "id" INTEGER NOT NULL,
  "first_name" VARCHAR(50),
  "last_name" VARCHAR(50),
  PRIMARY KEY ("id")
);

CREATE TABLE "raw_orders" (
  "id" INTEGER NOT NULL,
  "user_id" INTEGER,
  "order_date" DATE,
  "status" VARCHAR(20),
  PRIMARY KEY ("id"),
  CONSTRAINT "fk_orders_customer" FOREIGN KEY ("user_id") REFERENCES "raw_customers" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION
);

CREATE TABLE "raw_payments" (
  "id" INTEGER NOT NULL,
  "order_id" INTEGER,
  "payment_method" VARCHAR(20),
  "amount" INTEGER,
  PRIMARY KEY ("id"),
  CONSTRAINT "fk_payments_order" FOREIGN KEY ("order_id") REFERENCES "raw_orders" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION
);

CREATE INDEX "raw_orders_user_id" ON "raw_orders" ("user_id");
CREATE INDEX "raw_payments_order_id" ON "raw_payments" ("order_id");
