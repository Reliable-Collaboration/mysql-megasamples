-- postgres DDL for `jaffle_shop`, generated by megasamples from the MySQL corpus. Do not edit:
-- the next port rewrites it. Data is loaded from the MySQL Shell dump, not from here.

CREATE TABLE "raw_customers" (
  "id" bigint NOT NULL,
  "first_name" character varying(50),
  "last_name" character varying(50),
  PRIMARY KEY ("id")
);

CREATE TABLE "raw_orders" (
  "id" bigint NOT NULL,
  "user_id" bigint,
  "order_date" date,
  "status" character varying(20),
  PRIMARY KEY ("id")
);

CREATE TABLE "raw_payments" (
  "id" bigint NOT NULL,
  "order_id" bigint,
  "payment_method" character varying(20),
  "amount" integer,
  PRIMARY KEY ("id")
);

CREATE INDEX "raw_orders_user_id" ON "raw_orders" ("user_id");
CREATE INDEX "raw_payments_order_id" ON "raw_payments" ("order_id");

ALTER TABLE "raw_orders" ADD CONSTRAINT "fk_orders_customer" FOREIGN KEY ("user_id") REFERENCES "raw_customers" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION;
ALTER TABLE "raw_payments" ADD CONSTRAINT "fk_payments_order" FOREIGN KEY ("order_id") REFERENCES "raw_orders" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION;
