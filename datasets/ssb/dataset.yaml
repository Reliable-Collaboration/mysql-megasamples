name: ssb
database: ssb
tier: generated
record: knowledge/datasets/ssb.md
licenses: [tpc-eula]
# Nothing downloaded, nothing committed: the generator is compiled in engines/mysql/loader.Dockerfile and
# its output stays on the machine that produced it.
artifacts: []
stage: {input: none, scale_factor: true}
load:
  - ssb.sql
