<!-- Generated by scripts/gen_provenance.py from knowledge/ and manifest.yaml.
     Do not edit by hand: `make provenance` overwrites it. -->

# Provenance — `wideworldimporters`

**Upstream:** https://github.com/microsoft/sql-server-samples/releases/tag/wide-world-importers-v1.0

**Knowledge record:** [`knowledge/datasets/wideworldimporters.md`](../../knowledge/datasets/wideworldimporters.md)

**Licence:** mit, natural-earth-public-domain, us-government-public-domain — see `LICENSE` beside this file.

## Source artifacts

Every byte this database is built from, pinned. `scripts/fetch.py` refuses to proceed if
a download does not match.

| artifact | bytes | sha256 |
|---|---:|---|
| `wideworldimporters/WideWorldImporters-Standard.bak` | 126,951,424 | `066279a8cd28c8d85cbd8215ea71a5d672b420cfbc19756b635c27bd8027dada` |

## What upstream publishes

Release `wide-world-importers-v1.0` (2016-06-08; no auth; no checksums): `WideWorldImporters-Standard.bak` **126,951,424 B** (chosen), `WideWorldImporters-Full.bak` 127,111,168, `-Standard.bacpac` 60,996,994, `-Full.bacpac` 61,291,839, plus `_old` variants of each. Learn: "For the Full version of the sample, use SQL Server Evaluation/Developer/Enterprise Edition"; the Standard edition omits In-Memory OLTP/columnstore/partitioning/PolyBase/full-text which `Application.Configuration_*` procedures can re-apply. **Inferred:** data content is identical between Standard and Full (same generation run); the Standard .bak restores on any edition. Pin by release asset name + measured sha256 at first download.

## Licence and attribution, as recorded

MIT via repository `license.txt` ([license record](/licenses/mit.md)); Learn: "View the license for the sample database and the sample code. The sample database includes public data that's been loaded from data.gov and Natural Earth data." -> also cite [Natural Earth public domain](/licenses/natural-earth-public-domain.md) (no attribution required) and note data.gov as US-government public data. Attribution string: "WideWorldImporters sample database, Copyright (c) Microsoft Corporation, MIT License; includes public-domain geographic data from Natural Earth and data.gov." No real personal data (fictitious names; hashed passwords are sample values).

## Build-time software and its licence

Re-deriving this database from upstream runs software under a licence you must
accept: **Microsoft SQL Server Developer edition EULA (Linux container, ACCEPT_EULA)** (https://go.microsoft.com/fwlink/?linkid=857698).

`https://go.microsoft.com/fwlink/?linkid=857698` -> `License_Dev_Linux.rtf` ([source record](/sources/microsoft-sql-server-developer-linux-eula.md)); the served document is titled "MICROSOFT SOFTWARE LICENSE TERMS - MICROSOFT SQL SERVER 2017 DEVELOPER". The MCR catalog also links the general SQL Server product terms `https://www.microsoft.com/licensing/terms/productoffering/SQLServer/EAEAS#LicenseModel`.

> BY USING THE SOFTWARE, YOU ACCEPT THESE TERMS. IF YOU DO NOT ACCEPT THEM, DO NOT USE THE SOFTWARE.
> ... to design, develop, test and demonstrate your programs. You may not use the software on a device or server in a production environment.
> Your end users may access the software to perform acceptance tests on your programs.

Nothing licensed under those terms is redistributed here: the container is deleted
when the export finishes, and only the exported data — which carries the licence
named above — is kept. **Using the published image does not involve it**, and
neither does building any other dataset.

The maintainer accepted these terms explicitly, and the acceptance is recorded rather than implied:

* `scripts/mssql.py` **refuses to start the container** until `MEGASAMPLES_ACCEPT_MSSQL_EULA=1` is set
  or `--accept-eula` is passed, and prints the terms and the link first. `ACCEPT_EULA=Y` is set only
  after that.
* The run records how acceptance was given, alongside the image digest and engine build, in
  `downloads/<dataset>/export/server.json`.
* Image: `mcr.microsoft.com/mssql/server@sha256:ba4c8329f48fb8f02e1416be6a930ebfd71268caee78aa985f3af4315e457c89`,
  SQL Server 2022 CU26 (16.0.4265.3) Developer Edition. Used for 49 s, then deleted.

## Transformation

Converted by [`datasets/wideworldimporters/convert.py`](convert.py). WideWorldImporters (OLTP) -> MySQL.

What was and was not ported:

* Website procedures (11: ActivateWebsiteLogon, ChangePassword, InsertCustomerOrders (TVP), InvoiceCustomerOrders, RecordColdRoomTemperatures (TVP), RecordVehicleTemperature (JSON), SearchFor*) -> port the JSON/search ones where feasible (`SearchForStockItemsByTags` uses `OPENJSON`; MySQL `JSON_TABLE` can replace), drop TVP-based ones (MySQL has no table-valued parameters) with stubs.
* `Application.Configuration_*`, `DataLoadSimulation.*`, `Sequences.Reseed*`, `Integration.*` (ETL Get*/procs) -> drop (SQL Server feature configuration / simulation).
* Views in Website/Reports/PowerBI schemas (few) -> port if simple.
* Full-text indexes (applied by `Configuration_ApplyFullTextIndexing`, not in Standard by default) -> optional InnoDB FULLTEXT on People.SearchName, StockItems.SearchDetails.
* 100s of `Description` extended properties -> `COMMENT`s.

## Measured

48 tables, 4,713,833 rows. Per-table counts, digests, index sets,
query plans and canonical query results are pinned under `tests/` and are
checked on every build.
