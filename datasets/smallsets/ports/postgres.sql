-- postgres DDL for `smallsets`, generated by megasamples from the MySQL corpus. Do not edit:
-- the next port rewrites it. Data is loaded from the MySQL Shell dump, not from here.

CREATE TABLE "iris" (
  "iris_id" bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  "sepal_length" numeric(3,1),
  "sepal_width" numeric(3,1),
  "petal_length" numeric(3,1),
  "petal_width" numeric(3,1),
  "species" character varying(12),
  PRIMARY KEY ("iris_id")
);

CREATE TABLE "penguins" (
  "penguins_id" bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  "species" character varying(12),
  "island" character varying(12),
  "bill_length_mm" numeric(4,1),
  "bill_depth_mm" numeric(4,1),
  "flipper_length_mm" integer,
  "body_mass_g" integer,
  "sex" character varying(6),
  "year" integer,
  PRIMARY KEY ("penguins_id")
);

CREATE TABLE "penguins_raw" (
  "penguins_raw_id" bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  "study_name" character varying(12),
  "sample_number" integer,
  "species" character varying(60),
  "region" character varying(20),
  "island" character varying(20),
  "stage" character varying(30),
  "individual_id" character varying(10),
  "clutch_completion" character varying(3),
  "date_egg" date,
  "culmen_length_mm" numeric(4,1),
  "culmen_depth_mm" numeric(4,1),
  "flipper_length_mm" integer,
  "body_mass_g" integer,
  "sex" character varying(6),
  "delta_15_n" numeric(10,6),
  "delta_13_c" numeric(10,6),
  "comments" character varying(255),
  PRIMARY KEY ("penguins_raw_id")
);

CREATE TABLE "titanic" (
  "titanic_id" bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  "pclass" smallint,
  "survived" smallint,
  "name" character varying(100),
  "sex" character varying(6),
  "age" numeric(6,4),
  "sibsp" smallint,
  "parch" smallint,
  "ticket" character varying(20),
  "fare" numeric(9,4),
  "cabin" character varying(40),
  "embarked" character(1),
  "boat" character varying(8),
  "body" integer,
  "home_dest" character varying(100),
  PRIMARY KEY ("titanic_id")
);
