<!-- Generated by megasamples/provenance.py from knowledge/ and manifest.yaml.
     Do not edit by hand: `make provenance` overwrites it. -->

# Provenance — `oracle_co`

**Upstream:** https://github.com/oracle-samples/db-sample-schemas/tree/v23.3/customer_orders

**Knowledge record:** [`knowledge/datasets/oracle-co.md`](../../knowledge/datasets/oracle-co.md)

**Licence:** mit — see `LICENSE` beside this file.

## Source artifacts

Every byte this database is built from, pinned. `megasamples/fetch.py` refuses to proceed if
a download does not match.

| artifact | bytes | sha256 |
|---|---:|---|
| `oracle_co/co_create.sql` | 19,570 | `8ce42790ec255840bcaf22ff8bc4f53e53996ccecb318410a0b3a36a4c3d6cc1` |
| `oracle_co/co_populate.sql` | 1,274,129 | `e636942e49d9f7cb586f779122ffa2b4da95b07cc7f314c0ef1dae60a5f0cefa` |

## What upstream publishes

* Same repository/tag/commit as [HR](/datasets/oracle-hr.md) (v23.3 = `e3325a8`). Files (bytes): `customer_orders/co_install.sql` 8,141; `co_create.sql` 19,570; **`co_populate.sql` 1,274,129** (8,980 lines); `co_uninstall.sql` 2,917; README.md 2,384. Directory total 1,309,608 bytes. No auth/click-through. Schema version "21.1", release date 08-FEB-2022.
* Checksums: none upstream; executor records sha256 of the fetched files.

## Licence and attribution, as recorded

MIT — [MIT record](/licenses/mit.md); same attribution string as HR.

## Transformation

Converted by [`datasets/oracle_co/convert.py`](convert.py). oracle_co: thin wrapper over the shared Oracle converter.

What was and was not ported:

| object | action | reason |
|---|---|---|
| view `customer_order_products` | port with change | `LISTAGG(... ON OVERFLOW TRUNCATE '...' WITH COUNT) WITHIN GROUP (ORDER BY line_item_id)` → `GROUP_CONCAT(product_name ORDER BY line_item_id SEPARATOR ', ')`; overflow behaviour differs (MySQL silently cuts at `group_concat_max_len`, default 1024 — set 65535 in the session or accept; longest order has few items) |
| view `store_orders` | port with change | `GROUP BY GROUPING SETS (...)` + `grouping_id()`: **verified 2026-09-02 on `mysql:9.7.2`** — `GROUPING SETS` parses but is rejected at execution with `ERROR 3889 (HY000): Secondary engine operation failed`, i.e. it is a HeatWave-only feature and unusable on community MySQL. `WITH ROLLUP` + `GROUPING()` works (verified). So: port with `ROLLUP` and a `CASE` on `GROUPING(store_name), GROUPING(order_status)`; the row set (store totals, status totals, grand total) can be reproduced with a `UNION ALL` if needed |
| view `product_reviews` | port | `JSON_TABLE` + `AVG() OVER (PARTITION BY ...)` (**Inferred:** both supported in 8.0+/9.x, verify) |
| view `product_orders` | port | plain aggregate |
| CHECK constraints (5) | port | including `store_at_least_one_address_c` (**Inferred:** enforced CHECK since 8.0.16) |
| identity restart block | drop | AUTO_INCREMENT handles it |
| `COMMENT ON` (all tables/columns/views) | port | table/column comments; view-column comments have no MySQL equivalent → put into the view's own `COMMENT`? MySQL views have no COMMENT clause → keep them in the generated DDL as SQL comments |

## Measured

7 tables, 8,783 rows. Per-table counts, digests, index sets,
query plans and canonical query results are pinned under `tests/` and are
checked on every build.
